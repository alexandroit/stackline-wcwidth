# Compatibility

The canonical compatibility definition is `COMPATIBILITY_CONTRACT.md`. It
records the exact `wcwidth@1.0.1` API retained by this package and every
intentional Unicode 17.0.0 behavior change.
